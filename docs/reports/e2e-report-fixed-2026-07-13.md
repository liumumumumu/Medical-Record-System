# 医疗病历生成与分析系统：修复后全链路联调报告

日期：2026-07-13

环境：Windows 11、Java 21、Python、Node.js、MongoDB 8

结论：自动化测试、构建、数据流水线、真实四服务 HTTP 链路、DOCX 内容与版面、启动停止和交付打包均通过。

## 1. 本轮修复范围

- 默认使用真实 Flask AI；Windows 根目录脚本可一键启动和停止全部服务。
- 历史关键字改为 MongoDB 服务端搜索，不再仅过滤当前页。
- 注册密码按 bcrypt UTF-8 72 字节上限校验，避免中文长密码触发 500。
- 注销令牌哈希持久撤销，旧 Token 在自然过期前也不可再用。
- PDF、DOC、DOCX 提取正文并加入 AI 辅助检查上下文；图片无 OCR 时明确标记 `metadata_only`。
- 附件可鉴权下载，解析正文和失败原因可在结果页查看。
- AI 输出真实动态置信度与低置信度原因，移除处理建议中的重复免责声明。
- DOCX 科室本地化，免责声明只输出一次。
- 生成病历保留换行，年龄 `0` 正常显示。
- 上传数量、单文件大小和总大小在前后端保持一致，前端展示具体失败原因。
- AI 健康状态进入 Spring Boot 聚合健康检查。
- 同时允许 `localhost` 与 `127.0.0.1` 前端来源。
- JWT 不再使用已知默认密钥；演示用户和 Swagger 默认关闭。
- 前端增加 Vitest；后端增加 Unicode 密码、附件解析、置信度与报告断言。
- 数据模块和在线 AI 的同义词资源对齐，更新派生特征文件。
- 根目录 Git 元数据恢复可用；保留前端 `CYH`、数据 `CYL` 两个既有子仓库历史，未提交或推送。
- 大型 `dataset/` 明确设为离线可选资源，交付包自动排除。

## 2. 自动化测试与构建

通过根目录 `scripts/run-all-tests.ps1` 执行：

| 模块 | 结果 |
| --- | --- |
| Flask AI | 23 项通过 |
| 数据处理 | 17 项及 4 个子测试通过 |
| NHANES 流水线 | 合并、标准化、质量分析、子集、校验五步通过；9254 行 |
| Spring Boot | 21 项通过，0 失败、0 错误 |
| React | 2 个测试文件、3 项通过 |
| 前端生产构建 | TypeScript + Vite 通过；3511 个模块 |

## 3. 真实四服务全链路

使用独立数据库 `medical_records_e2e_20260713` 启动：

```powershell
.\scripts\start-all.ps1 -MongoDatabase medical_records_e2e_20260713
python .\scripts\e2e-smoke.py --filler-count 21
.\scripts\stop-all.ps1
```

实测通过：

1. Flask `/health` 返回 `ok`，Spring Boot 聚合健康返回 `UP`，前端返回 HTTP 200。
2. `127.0.0.1:5173` CORS 预检通过。
3. 30 个中文字符的超限密码返回 HTTP 400，而非后端 500。
4. 注册成功并签发 JWT。
5. multipart 上传真实 DOCX，附件状态为 `parsed`。
6. 附件中的“白细胞轻度升高”进入 AI 生成病历，证明不是只保存元数据。
7. AI 模型版本为 `1.0.0`，本次置信度为 `0.788555`，并非前端硬编码值。
8. 年龄 `0` 在输入、结果和报告中完整保留。
9. 目标病例后创建 21 条记录，使其落到历史首页之外；按患者名搜索仍返回该病例，证明搜索在服务端执行。
10. 两行人工编辑内容保存成功，并进入后续 DOCX。
11. 报告和原附件均通过鉴权下载；附件字节与上传文件完全一致。
12. 注销返回 204；旧 Token 再访问 `/auth/me` 返回 401；重新登录签发新 Token。
13. 默认 `/v3/api-docs` 不公开。
14. `start-all.ps1` 和 `stop-all.ps1` 均通过，结束后三个服务端口全部释放，MongoDB 系统服务保留。

联调结束后已删除独立测试数据库，没有向默认 `medical_records` 写入测试病例。

## 4. DOCX 与运行日志

- 报告成功转为 PDF 并渲染为单页 PNG检查。
- 中文无乱码，无文本裁切、重叠或分页异常。
- 科室显示“内科”，人工编辑的两行内容都存在，免责声明恰好一次。
- AI、后端、前端共 6 个运行日志中未检出 `ERROR`、`Exception`、`Traceback`、`Unhandled` 或 `WARN`。

## 5. 启动与交付验证

- 四个 PowerShell 脚本已统一为 Windows PowerShell 5 可识别的 UTF-8 BOM。
- `prepare-delivery.ps1` 已真实生成并校验压缩包：279 个条目、8.32 MB。
- 压缩包包含根 README、启动脚本和在线 AI 模型；不包含 `.git`、`.runtime`、`dataset`、`node_modules`、构建目录、运行病例或本地密钥。
- 根目录 `dataset/` 共 8544 个文件、约 28.86 GB，不是在线运行依赖。

## 6. 验收限制

本轮曾尝试使用 Codex 内置浏览器做可见页面点击验收，但该客户端两次异常关闭 Codex 主窗口，因此按用户要求停止所有浏览器和界面控制。此限制属于验收工具异常，不是项目服务异常；上述编译、自动化测试、真实 HTTP 全链路、文件下载和 DOCX 渲染均已完成。

## 7. 当前状态

- 本轮代码均在本机工作区，未提交、未推送。
- 在已执行范围内未发现新的阻断性代码问题。
- 本地联调服务已停止，MongoDB 保持系统原状态。
